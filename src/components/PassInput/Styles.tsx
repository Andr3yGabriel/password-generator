import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    minWidth: 150,
    height: 40,
    color: 'black',
    backgroundColor: 'white',
    borderColor: 'gray',
    borderWidth: 1.5,
    borderRadius: 5,
    margin: 10,
    padding: 10,
    textAlign: 'center'
  }
});